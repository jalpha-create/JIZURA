# WORKLOG — JIZURA（jalpha-create フォーク）

## 2026-09-24 フォークと開発環境の用意

- `852wa/JIZURA` を `E:\jcreate_app\JIZURA` にクローンし、`jalpha-create/JIZURA` にフォーク。
  - remote: `origin` = 自分のフォーク / `upstream` = 元リポジトリ（push は `DISABLED` にして誤送信を防止）。
  - 元の更新の取り込み: `git fetch upstream && git merge upstream/main`
- `python build.py` の出力がコミット済みの `index.html` / `en/index.html` と一致することを確認（ビルドの再現性あり）。
- `.gitignore` を追加（`__pycache__/` `dev/www/` `out/` `build/` `.claude/` など）。
- ローカル確認は `.claude/launch.json`（`python -m http.server 8765`、リポジトリ直下）。
- 方針（ユーザー回答）: 演出部品を増やす／制作フローを便利に／自分の案件（広告・縦型ショート・MV）向けに特化。自分の GitHub で公開する。
- 改造の原則: 新しい演出は `src/11p_<pack>.js` の追加で行い、元の本体ファイルはなるべく触らない（upstream の更新を取り込みやすくするため）。手順は `docs/EXPRESSION_PACKS.md`。

## 2026-09-24 起動ファイルと参考フォークの調査

- `JIZURA起動.cmd` を追加。ダブルクリックで `build.py` → `python -m http.server 8770 --bind 127.0.0.1`（最小化ウィンドウ）→ ブラウザ表示。ポートが使用中ならサーバー起動を省略。サーバー停止は最小化された「JIZURA server」ウィンドウを閉じる。
  - 待機は `timeout.exe` ではなく `ping -n 3`（入力のない環境で timeout がエラーになるため）。本文は ASCII＋CRLF。
- 優先順位（ユーザー指定）: ①複数画面比の一括書き出し → ⑤案のお気に入り保存 → ⑦自分用演出パック → ④縦型SNSセーフエリア → ②ブランドキット → ③タイトル/CTAカード。
- 参考: `hirazisora/JIZURA`（MIT・upstream から37コミット先行／2コミット遅れ）。追加機能は画像・動画の背景/前景レイヤー、クロマキー、行ごとの歌詞表示エリア、タイムライン境界のドラッグ編集、アンドゥ/リドゥ、空カット挿入、Jev（TypeSafe API に歌詞を送る外部AI選定・要APIキー）。
  - `git fetch https://github.com/hirazisora/JIZURA.git main:refs/remotes/hirazi/main` で取得済み。試しにマージすると衝突は src/09_render.js・11_export.js・12_ui.js・app/* と生成物（index.html・CEP zip）。upstream 側の差分は計100行程度で、解消は現実的。

## 2026-09-24 hirazi fork の取り込みと改名

- ユーザー判断で `hirazisora/JIZURA` を丸ごとマージ（ブランチ `merge/hirazi`）。衝突は両方の機能を残す形で解消（詳細はマージコミット）。
  - 取り込んだ機能: 画像・動画の背景／前景レイヤー、クロマキー、行ごとの歌詞表示エリア、タイムライン境界ドラッグ、アンドゥ／リドゥ、空カット、Jev（β）。
  - 独自修正: 透過PNGの前景／後景書き出しで、前景素材を後景レイヤーに描かない（`src/09_render.js`）。
  - 確認: `node dev/media_test.js` `lyric_test.js` `jev_test.js`、`python dev/jev_server_test.py` すべて成功。ブラウザで16:9/9:16/1:1 × 通常・透過・後景・前景の計360フレームを描画して例外なし。日英の画面でコンソールエラーなし。
- 表記を「jAlpha edition」に変更。公開URL・Jev の接続元を `https://jalpha-create.github.io` に変更。「利用について」には自分の版＋取り込み元（hirazi fork）＋オリジナルのクレジットを併記（MIT の表示条件）。
- Jev は外部サービス（TypeSafe API）に歌詞を送る。キーを設定して `jev_server.py` を起動しない限り動かない。
- upstream（852wa）の更新を取り込むときは、hirazi 版で大きく変わった `src/12_ui.js` `src/09_render.js` `app/body.html` が衝突しやすい。

## 2026-09-24 ① 複数画面比のまとめて書き出し

- 詳細モードの「出力」タブと、かんたんモードの書き出し欄に「まとめて書き出し」を追加（`src/12_ui.js` の `runBatchExport` ほか、`app/body.html`、`app/style.css` 末尾）。
  - 画面比をチップで複数選択（既定 16:9・9:16・1:1）。選択は `project.batchAspects` に保存。
  - 画面比ごとに `Object.assign({}, S.project, { aspect })` から `J.plan` で組み直し（同じシード。`J.plan` はプロジェクトを書き換えないことを確認済み）、既存の `J.exportMP4` で書き出し → `名前_16x9.mp4` で1本ずつ保存。
  - 英訳は `app/english.py` の `JALPHA_BODY` / `JALPHA_UI`（自分の版の追加文言はここにまとめ、FORK/本家の表より先に適用）。
- 確認: 保存処理を差し替えて720pで3本書き出し（約9秒）。出力は 1280×720 / 720×1280 / 720×720、長さはどれも12.17秒でタイムラインと一致。中間フレームで各画面比向けに組み直されていることを目視。中止で「キャンセルしました」表示・ボタン復帰。日英のUI文言に訳漏れなし。
- 未対応: PNG系のまとめて書き出し、画面比ごとの素材配置の個別調整（素材・歌詞エリアは画面比率の割合で持つので、そのまま伸縮する）。

## 2026-09-25 将来計画: 説明動画（インフォグラフィック）ツール

- 参考: @ponzponz15 の投稿（2026-09-22、黄色一色×黒線画のモーショングラフィックス。滑車・歯車・小キャラが動き「好き。→つくってみたい。→好きが、つくれるに変わる。」→ロゴ）。
- ユーザー決定:
  - **別アプリとして作る**（同じリポジトリ内の別ページ。JIZURA の描画・書き出し・フォント・音声の仕組みを流用）。
  - 用途は全部: 広告・サービス紹介／解説・ハウツー／データ・レポート／SNS縦型ショート。
  - **着手は JIZURA のロードマップ（①⑤⑦④②③）を一通り終えてから**。
- 構想: 台本（1ブロック＝1シーン）→ シーンの型（タイトル・箇条書き・数字カウントアップ・棒/円グラフ・手順フロー・比較・年表・引用・ロゴ/CTA）× 絵柄テーマ（線画イエロー・コーポレート・ポップ・ダークテック）× シードで複数案。キャラの一点物の芝居は対象外（AI動画側の領分）。定番の小さな動き（キャラが手を振る、滑車で看板が降りる等）は装飾部品として用意する。

## 2026-09-25 ⑤ 案のお気に入り保存

- 「見た目」（◀▶ 履歴と同じ `HKEYS`: style, mood, seed, fx, enabled, fonts, colors, overrides）を名前付きで保存・呼び戻す機能を追加（`src/12_ui.js` の「お気に入り (jAlpha edition)」ブロック、`app/body.html`、`app/style.css` 末尾）。
  - 保存先はブラウザの localStorage `jizura.favorites.v1`（プロジェクトとは別。別の曲にも使い回せる）。最大100件。サムネイルは最初の歌詞カットの60%地点を 240px 幅 JPEG（約5KB）で保存。
  - `overrides`（行ごとの鍵・指定）は歌詞のハッシュが一致するときだけ戻す。違う歌詞では今の行指定を残し、トーストで知らせる。
  - 呼び戻しは `remember()`→適用→`commit()` なので ◀ で戻れる。
  - UI: 再生バーの ☆、かんたんモードの「お気に入り」欄、詳細モードのスタイルタブ最上部（折りたたみ・ファイル保存/読み込み付き）。名前変更 ✎、削除 ×（確認あり）。
- 確認（ブラウザ）: 追加→おまかせで変更→呼び戻しで8項目すべて一致、◀▶ の往復、別の歌詞で行指定が残る、名前変更、ファイル保存→削除→読み込みで復元、重複読み込みの案内、不正ファイルのエラー、英語版の文言。

## 2026-09-25 ⑦A 配色・書体ライブラリの取り込み

- `tools/import_library.py` を追加: `E:/jcreate_app/palettes/palettes.js`（98配色）と `E:/jcreate_app/fonts/fonts.js`（Google Fonts 40書体、system 4書体は除外）を Node で評価して `src/11r_library.js`（生成物）に書き出す。ライブラリ更新時は再実行→`build.py`。
- `src/11s_library.js`（ロジック）:
  - 書体: `J.FONTS.lib_fXX` として登録。既存と同じ family+weight は除外（結果29書体）。`extra: true, lib: true`。ランダム候補は `project.lib === true` かつ和文書体だけ（`J.randomOk` をラップ）。
  - 配色: `project.colors.palette = { id, v, cols }` → `J.paletteSchemes` がスタイルの配色の組を丸ごと置き換える（背景候補は暗い順／明るい順の交互で v 番目。2つ目は最もコントラストの高い色、3つ目は最も鮮やかな色）。文字は 4.5 以上（届かなければ白か純黒）、アクセントは 2.4 以上。`J.resolveStyle`（src/04_styles.js）に1行フックを追加し、ユーザーの色上書きはその上に効く。
  - 全98配色×全背景パターン×3スタイル＝3136組で文字コントラスト最小 4.5・アクセント最小 2.4 を確認（最初は #111111 のフォールバックで 4.38 の組があり、純黒に修正）。
- UI（`src/12_ui.js`）: スタイルタブに「配色ライブラリ」（カテゴリー絞り込み・ランダム・背景切り替え・解除）、おまかせの下に「配色・書体ライブラリも使う」（`project.lib`）。おまかせはオン時に約6割でライブラリ配色、オフ時は配色ライブラリを外す。「ここだけ変える→配色」はオン時ライブラリから。いまの案に配色IDを表示。
- AE: `node tools/export_ae_data.js` で `ae/data.json` にライブラリ書体も入り、AE 側は未知キーを family 名で探す（PCに入っていれば使える）。AE パネルのテスト（`dev/ae_test.js`、要 `npm install` acorn）は未実行。
- 英語版: カテゴリー名は英訳、配色名は日本語のみなので英語版は「P24 · Cool」表記。
- ⑦の B（案件向けスタイル新規）・C（新しい動き）は後日（ユーザー指示）。

## 2026-09-25 ④ 縦型SNSのセーフエリア表示

- 再生バーに「セーフエリア」選択（なし／TikTok／Instagram リール／YouTube ショート／SNS 3つ重ね／放送用セーフ）。プレビューのみ（DOM の SVG オーバーレイ `#safeOverlay`、書き出しのキャンバスには描かない）。選択は localStorage `jizura.safeArea`。
  - 目安値（1080×1920基準）: TikTok 上160/右150/下480/左60、リール 上220/右140/下420/左60、ショート 上190/右190/下440/左60。「3つ重ね」は各辺の最大。アプリの画面は変わるので README で実機確認を促している。
  - 隠れる範囲を薄い赤、アプリUIの形（上部タブ・右のボタン列・下のキャプション）を簡易図形で描き、セーフエリアを点線で表示。9:16 以外では「9:16 用です」と表示。放送用はアクション93%・タイトル90%。
  - `sizeViewport()` から位置合わせと再描画（replan・リサイズ・画面比変更で追従）。オーバーレイ位置はビューポートの枠線ぶん（clientLeft/Top）を差し引く（最初1pxずれていた）。
- 確認: 16:9・9:16・1:1・リサイズ後でキャンバスと完全一致、枠の数値、モード切替と記憶、英語版の文言、コンソールエラーなし。9:16 の3つ重ねで、歌詞の末尾が右のボタン列にかかる例を目視で確認。

## 2026-09-25 ② ブランドキットは保留

- ユーザー判断で見送り（今は作りたいものがない）。必要になったら、登録内容（ロゴ／固定色／固定書体）と複数ブランドの切り替え要否を聞いてから作る。
